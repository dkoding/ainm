# Deterministic Task Checklist

Status: complete for the documented `/solve` competition scope and the verified deterministic runtime backlog.

This file now reflects the verified state after adding deterministic salary payroll and bank reconciliation execution.

## Completed

- [x] The LLM is the only active natural-language interpreter in the runtime path.
- [x] The handoff object is the versioned `TaskAnalysis` contract.
- [x] Downstream code no longer rewrites prompt meaning into different workflows.
- [x] Planner-contract validation rejects semantically wrong wrapper selections instead of silently repairing them in code.
- [x] Startup coverage audit logging reports coded versus wrapper-only method counts.

## Deterministic Solve Coverage

- [x] Curated deterministic workflows exist for employees, customers/products, invoicing, travel expenses, projects, corrections, departments, salary payroll, and bank reconciliation.
- [x] Wrapper-only methods remain available for deterministic resource-shaped CRUD/search/delete/reverse/update tasks when no narrower curated workflow is required.
- [x] Salary payroll requests now route through `RunSalaryPayrollWorkflow`, resolve employee and salary types deterministically, and post `POST /salary/transaction`.
- [x] Bank reconciliation requests now route through `RunBankReconciliationWorkflow`, resolve counterparties and invoices deterministically, and register outgoing and supplier-invoice payments.
- [x] Supplier-invoice PDF registration remains covered by the curated supplier-invoice workflow; observed `403` failures are treated as Tripletex access preconditions, not coverage gaps.

## Planner And Extraction

- [x] Planner instructions explicitly require multilingual translation into canonical method names and argument keys.
- [x] Planner instructions now explicitly require exact payroll-period fields for payroll tasks.
- [x] Planner instructions now explicitly require structured `statementEntries` for bank reconciliation tasks.
- [x] Planner examples and contract fixtures now cover payroll and bank reconciliation in addition to the earlier workflows.

## Validation And Regression Coverage

- [x] Unit tests cover curated salary payroll routing.
- [x] Unit tests cover bank reconciliation routing for outgoing customer invoices.
- [x] Unit tests cover bank reconciliation routing for supplier invoices.
- [x] Solver tests verify that valid structured salary and bank workflows reach deterministic execution instead of failing as input or wrapper-exhaustion errors.
- [x] Planner-contract tests reject `RunSalaryOpenAPIWorkflow` and `RunBankOpenAPIWorkflow` when the structured task requires the curated workflows.

## Coverage And Release Gate

- [x] Local verification reports `84` total methods, `27` coded methods, `57` wrapper-only methods, and `0` documented category gaps.
- [x] `coverage_report.json` and `competition_dashboard.json` are generated by the release-gate script.
- [x] The release gate runs compile checks and unit tests before deployment.

## Interpretation Boundary

- [x] Code only consumes structured task fields after planner handoff.
- [x] Execution-time lookup heuristics are limited to deterministic API-resolution behavior such as ID lookup, amount matching, date-window fallback, and postcondition checks.
- [x] Code does not read raw prompt text to infer workflow family or rewrite user intent.

## Done Definition

- [x] The remaining deterministic runtime gaps from the latest production logs are closed.
- [x] The backlog for the documented `/solve` competition scope is complete.
- [x] Remaining runtime failures are external Tripletex permission or account-state preconditions, not missing deterministic workflow code.
